@model DataAccess.Entity.Genre
@{
    ViewData["Title"] = "DeleteGenre";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="panel">
            <div class="alert alert-danger">
                <form method="post">
                    <label asp-for="GenreName"></label>
                    <h2> Türünü silmek istediğinizden emin misiniz?</h2>
                    <span>Silmek istediğinize emin misiniz?</span>
                    <a asp-controller="Genre" asp-action="Index" type="submit" class="btn btn-danger"> evet sil</a>
                    <a asp-controller="Genre" asp-action="Index" class="btn">İptal</a>
                </form>
            </div>
        </div>
    </div>
</div>
